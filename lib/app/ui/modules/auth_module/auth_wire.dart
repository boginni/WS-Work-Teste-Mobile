import 'package:flutter/material.dart';

class AuthWire extends StatelessWidget {
  const AuthWire({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
