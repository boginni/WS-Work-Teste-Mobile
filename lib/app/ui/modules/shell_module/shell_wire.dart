import 'package:flutter/material.dart';

class ShellWire extends StatelessWidget {
  const ShellWire({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
