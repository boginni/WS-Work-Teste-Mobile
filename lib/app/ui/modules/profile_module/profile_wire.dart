import 'package:flutter/material.dart';

class ProfileWire extends StatelessWidget {
  const ProfileWire({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
