import 'package:flutter/material.dart';

class SplashWire extends StatelessWidget {
  const SplashWire({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
